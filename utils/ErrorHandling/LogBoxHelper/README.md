# LogBoxHelper (Deprecated)

LogBox integration has been removed from the app, so this helper now does nothing. It remains only to avoid breaking existing imports; calling `LogBoxHelper()` will not change any runtime behavior.
